part of '../dashboard_tab.dart';

class _TypeBadge extends StatelessWidget {
  final String value;

  const _TypeBadge([this.value = '']);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
